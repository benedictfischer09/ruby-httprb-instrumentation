RSpec.describe Ruby::Httprb::Instrumentation do
  it "has a version number" do
    expect(Ruby::Httprb::Instrumentation::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
